## Plan

Overwrite `src/components/HowItWorksModal.tsx` with the full contents of the uploaded file `user-uploads://How_it_works_modal.txt` (1658 lines).

### Steps
1. Copy `user-uploads://How_it_works_modal.txt` over `src/components/HowItWorksModal.tsx` (overwrite).
2. Verify the file compiles (typecheck runs automatically).

No other files are touched. No dependency changes.